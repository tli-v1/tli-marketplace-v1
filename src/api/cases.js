export const fetchCases = () => Promise.resolve({ data: [], error: null })

export const getCaseDocSignedUrl = async (doc) => {
  return null
}

export const hydrateCaseDocuments = async (caseRows = []) => {
  return []
}
